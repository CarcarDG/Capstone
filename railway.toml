[build]
builder = "NIXPACKS"
buildCommand = "cd backend && mvn clean package -DskipTests"

[deploy]
startCommand = "cd backend && java -Dspring.profiles.active=prod -jar target/recipe-platform-1.0.0.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
